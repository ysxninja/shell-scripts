#!/bin/sh

# you can use the function below instead in your bashrc or zshrc
# cf() { du -a ~/.config/ | awk '{print $2}' | fzf | xargs -r $EDITOR }

du -a ~/.config/ | awk '{print $2}' | fzf | xargs -r $EDITOR 
